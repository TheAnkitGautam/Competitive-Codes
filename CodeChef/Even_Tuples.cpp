#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, q, l, r;
    cin >> t;
    while (t--)
    {
        int arr[n];
        cin >> n >> q;
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        while (q--)
        {
            cin >> l >> r;
            
        }
    }
    return 0;
}